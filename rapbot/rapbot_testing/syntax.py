import time
import timer
import random as rn

def main():
    print(syntax)
    print(battle)

def sleep2():
    time.sleep(2)
sleep = sleep2()

def syntax():
    nouns = "bad", "beach", "sweets"
    verbs = "go", "walk", "turn"
    adjectives = "blue", "round", "old"
    syntax = [(nouns, verbs, adjectives)]

syntax = syntax()
